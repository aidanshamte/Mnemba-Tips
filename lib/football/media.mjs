// Only dedicated player URLs are accepted. Arbitrary web pages are never framed.
export function safeVideo(raw){try{const u=new URL(raw);if(u.protocol!=='https:'||u.username||u.password||u.port)return null;if(['vimeo.com','player.vimeo.com'].includes(u.hostname)&&/^\/(?:video\/)?\d{6,12}$/.test(u.pathname))return 'https://player.vimeo.com/video/'+u.pathname.split('/').at(-1)+'?dnt=1';let id;if(['www.youtube.com','youtube.com'].includes(u.hostname)&&u.pathname==='/watch')id=u.searchParams.get('v');else if(u.hostname==='youtu.be')id=u.pathname.slice(1);else if(['www.youtube.com','www.youtube-nocookie.com'].includes(u.hostname)&&u.pathname.startsWith('/embed/'))id=u.pathname.split('/')[2];if(!/^[\w-]{11}$/.test(id??''))return null;return `https://www.youtube-nocookie.com/embed/${id}?rel=0`; }catch{return null;}}
export function articleVideo(article){return article.media?.permission==='publisher-embed'&&article.media?.articleUrl===article.url?safeVideo(article.media.url):null;}
