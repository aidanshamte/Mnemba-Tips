import test from 'node:test';import assert from 'node:assert/strict';import {statsBombLineups} from '../../lib/football/statsbomb-lineups.mjs';
test('recorded position intervals yield minutes and starters without inventing ratings',()=>{const f={home:{id:'statsbomb:1',externalId:'1',name:'Home'},away:{id:'statsbomb:2',externalId:'2',name:'Away'}},rows=[{team_id:1,lineup:[{player_id:10,player_name:'Starter',positions:[{from:'00:00',to:'45:00',from_period:1,start_reason:'Starting XI',position:'Center Back'},{from:'45:00',to:'90:00',position:'Right Back'}]},{player_id:11,player_name:'Substitute',positions:[{from:'60:00',to:'90:00',from_period:2,position:'Center Forward'}]},{player_id:12,player_name:'Unused',positions:[]}]}];const p=statsBombLineups(rows,f)[0].players;assert.equal(p[0].minutes,90);assert.equal(p[0].started,true);assert.equal(p[0].position,'DEF');assert.equal(p[1].minutes,30);assert.equal(p[1].started,false);assert.equal(p[2].minutes,null);assert.throws(()=>statsBombLineups([{team_id:3,lineup:[]}],f),/identity/);});
import {positionMinutes} from '../../lib/football/statsbomb-lineups.mjs';
test('final-whistle intervals require an observed end clock, including extra-time substitutes',()=>{const rows=[{from:'114:41',to:null,end_reason:'Final Whistle'}];assert.equal(positionMinutes(rows),null);assert.ok(Math.abs(positionMinutes(rows,122*60)-(7+19/60))<1e-8);assert.equal(positionMinutes(rows,90*60),null);assert.equal(positionMinutes([]),null);});
