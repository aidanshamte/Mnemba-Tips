'use client';
import {useState} from 'react';
export default function VerificationQueue({items}:{items:Record<string,any>[]}){const [message,setMessage]=useState('');async function verify(e:React.FormEvent<HTMLFormElement>,id:string){e.preventDefault();const values=new FormData(e.currentTarget);const r=await fetch('/api/intelligence',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'verify',id,decision:values.get('decision'),reference:values.get('reference')})});setMessage(r.ok?'Decision recorded. Reopen Diagnostics to refresh the queue.':'Verification could not be saved. Check the source reference.');}return <div className="intel-panel"><h2>Verification queue · {items.length}</h2><p>Review uncertain entity matches, official accounts and announcement candidates against the original source. Missing image text is never inferred.</p>{message&&<p role="status">{message}</p>}{items.map(item=><details key={item.id}><summary>{item.kind} · {JSON.parse(item.payload).name??JSON.parse(item.payload).title??item.entity_id}</summary><p><a href={item.source_url} target="_blank" rel="noreferrer">Review source ↗</a></p><pre style={{whiteSpace:'pre-wrap',overflowWrap:'anywhere'}}>{JSON.stringify(JSON.parse(item.payload),null,2)}</pre><form onSubmit={e=>void verify(e,item.id)} className="intel-filters"><label><input type="url" name="reference" required placeholder="https:// official verification reference" aria-label="Verification reference"/></label><select name="decision" aria-label="Verification decision"><option value="rejected">Reject candidate</option><option value="verified">Verified against reference</option></select><button type="submit">Record review</button></form></details>)}</div>}
