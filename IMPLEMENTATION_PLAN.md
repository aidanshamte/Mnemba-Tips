# Intelligence upgrade plan

Preserve existing source, credentials, basketball, and the football APIs. This export has no Git metadata; the pre-change source archive is `.sites-runtime/source-before-intelligence-upgrade.zip` and excludes credentials.

1. Add source permissions, conditional HTTP caching, provenance claims, canonical entities, conflict retention, social verification, and immutable snapshot migrations.
2. Add modular keyless OpenFootball JSON/text discovery, TheSportsDB v1 development metadata, Wikidata CC0 identities, permitted feeds, and archive availability metadata. Integrate existing keyed adapters without making credentials mandatory. Import live-available free sources; expose errors and queued coverage honestly.
3. Create evidence-time-safe snapshots, original trend/score features, chronological splits and optional probability calibration. Never backdate imported facts or fabricate missed snapshots.
4. Deliver a professional football home, archive fallback, entity pages, model/evidence panels, official social links, verification queue, and diagnostics. Preserve the existing lab at `/lab`.
5. Add serialized Windows update commands, Task Scheduler registration, consistent daily SQLite backups, structured logs and optional live smoke command.
6. Run mocked unit/contract/integration/API/migration/leakage tests, TypeScript, production build, and blocked-external-network desktop/mobile browser checks. Run real ingestion separately; leave the production server running.

Access restrictions and licensing are enforced. Unknown/prohibited automation is disabled. Restricted websites remain external verification links. Paid APIs and unauthorized media rehosting are excluded.
